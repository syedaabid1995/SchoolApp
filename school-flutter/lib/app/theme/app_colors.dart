import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const seed        = Color(0xFF2B4EFF); // vivid blue (matches screenshot)
  static const gradientEnd = Color(0xFF6B3FFF); // indigo-purple end
  static const success     = Color(0xFF15803D);
  static const warning     = Color(0xFFD97706);
  static const danger      = Color(0xFFB91C1C);
  static const neutral     = Color(0xFF475569);
  static const pageBackground = Color(0xFFF0F2F8); // light blue-grey page bg
}
